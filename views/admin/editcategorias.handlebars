

{{#each erros}}
    <div class=" alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h2>Editar Categoria:</h2>

<div class="card">

        <div class="card-body">

        <form action="/admin/categorias/edit" method="POST">
        <label for="nome">Nome:</label>
        <input type="hidden" name="id" id="id" value="{{categoria._id}}">
        <input type="text" name="nome" id="nome" placeholder="Nome da Categoria" class="form-control" value="{{categoria.nome}}">
        <label for="slug">Slug:</label>
        <input type="text" name="slug" id="slug" placeholder="Slug da Categoria" class="form-control" value="{{categoria.slug}}">

        <br>
        <button type="submit/" class="btn btn-success" >Alterar Categoria</button>
        
    </form>
            <form action="/admin/categorias" method="POST">
                <a href="/admin/categorias"><button type="submit" class="btn btn-danger mt-4">Cancelar Alteração</button></a>
            </form>
    </div>

</div>



<div class="jumbotron">
  <h1 class="display-4">Bem Vindo ao Blog do node</h1>
  <p class="lead">Olá a todos, este é uma simples aplicação baseada em um blog que fiz em NodeJS utilizando também MongoDB como banco de dados e o framework Bootstrap. Espero que gostem.</p>
  <hr class="my-4">
  <p>esta aplicação é usada apenas para fins academicos.</p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Cadastre-se</a>
  </p>
</div>

<hr>
<h2>Postagens recentes: </h2>

{{#each postagens}}
<div class="card mt-3">
        <div class="card-body">
                
                <h3>{{titulo}}</h3>
                <p>{{descricao}}</p>
                <a href='/postagem/{{slug}}'><button class="btn btn-warning">Leia Mais</button></a>
                <hr>
                <small>{{categoria.nome}}</small>
                 <small>Data de Publicação: {{data}}</small>
        </div>
</div>

{{else}}
    <h4>Nenhuma Postagem!</h4>
{{/each}}